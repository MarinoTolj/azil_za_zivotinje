import { Icon } from "@mdi/react";
import { mdiTrashCanOutline } from "@mdi/js";

const TrashIcon = () => {
  return <Icon path={mdiTrashCanOutline} size={1} color="red" className="" />;
};
export default TrashIcon;
